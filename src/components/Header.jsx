export default function Header() {
    return (
        <header>
            <h2>Expense Tracker</h2>
            <p>A simple expense tracker built with React framework.</p>
        </header>
    );
}
